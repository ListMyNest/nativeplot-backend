server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: jdbc:postgresql://db.jmhhmdlzqlwqxgfdrjpc.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: Trishika@2024
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    redis:
      url: redis://default:gQAAAAAAAQUDAAIncDFhNTlhNDFhODg4MTY0YjRmODkyNDZjYzdiZGNhNzg2N3AxNjY4MTk@thorough-pelican-66819.upstash.io:6379

jwt:
  secret: ${JWT_SECRET:local-dev-secret-key-replace-in-production-min-32-chars-okay}
  expiry-hours: ${JWT_EXPIRY_HOURS:24}
  admin-expiry-hours: ${ADMIN_JWT_EXPIRY_HOURS:8}

msg91:
  auth-key: ${MSG91_AUTH_KEY:}
  sender-id: ${MSG91_SENDER_ID:LSTNEST}
  otp-length: ${MSG91_OTP_LENGTH:6}
  otp-expiry-minutes: ${MSG91_OTP_EXPIRY_MINUTES:5}

supabase:
  url: ${SUPABASE_URL:}
  service-key: ${SUPABASE_SERVICE_KEY:}
  storage-bucket: ${SUPABASE_STORAGE_BUCKET:property-photos}

google:
  maps:
    api-key: ${GOOGLE_MAPS_API_KEY:}

firebase:
  service-account-json: ${FIREBASE_SERVICE_ACCOUNT_JSON:{}}

wati:
  api-token: ${WATI_API_TOKEN:}
  base-url: ${WATI_BASE_URL:https://live-server.wati.io}
  webhook-secret: ${WATI_WEBHOOK_SECRET:local-dev-webhook-secret}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}

app:
  base-url: ${APP_BASE_URL:http://localhost:8080}
